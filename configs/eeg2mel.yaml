group_name: "eeg2mel"
data_path: "/data/grad/users/datasets/NMED"
working_dir: "/data/grad/users/eeg_music"

eeg_window_size: 125
eeg_window_stride: 125

learning_rate: 1e-4
lr_scheduler: "cosine"
lr_warmup_steps: 3000
max_train_steps: 24000
save_every_steps: 1000
max_grad_norm: 1.0

batch_size: 550
val_batch_size: 50
validation_steps: 500

seed: 622
cache_dir: "./cache"
gradient_accumulation_steps: 1
mixed_precision: "bf16"
resume_from_checkpoint: null # "./results/eeg_music_align/27-06-2025-12:02:49"
data_split_type: "block"
